apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    run: redis-deployment
  name: redis-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      run: redis-deployment
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        run: redis-deployment
    spec:
      containers:
      - image: redis:alpine
        name: redis-deployment
        resources: {}
        volumeMounts:
        - mountPath: /data
          name: redis-data
      volumes:
        - name: redis-data
          emptyDir:
status: {}
