apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    run: result-deployment
  name: result-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      run: result-deployment
  strategy: {}
  template:
    metadata:
      labels:
        run: result-deployment
    spec:
      containers:
      - image: kodekloud/examplevotingapp_result:before
        name: result-deployment
        resources: {}
status: {}
